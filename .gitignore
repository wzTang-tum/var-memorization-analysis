# model weights and external source (see README for how to obtain these)
/checkpoints/
/VAR/

# ImageNet training images (gated dataset, not for redistribution)
/data/images/
/data/subset_1000.json
/data_N10000/

# figures/results regenerated locally by the pipeline (some contain
# ImageNet thumbnails and must not be committed; see README)
/figures/

# large intermediate activation caches regenerated by 03_compute_activations.py
/results/unitmem_N10000_global.npz
/results/unitmem_old_subset_within_N10000.npz
/results/regression1000_unitmem.npz
/results/*view_hashes*.csv
/results/checkpoint_N10000.pkl

# LaTeX build artifacts
report/*.aux
report/*.bbl
report/*.blg
report/*.log
report/*.out
report/*.fls
report/*.fdb_latexmk

__pycache__/
*.pyc
.DS_Store
